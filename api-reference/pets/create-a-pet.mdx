---
title: "Create a pet"
description: "Creates a pet record linked to a contact. Requires the pets.write scope."
openapi: "POST /pets"
---

Creates a pet and links it to an existing contact (owner). A pet cannot exist without a valid owner.

**Required scope:** `pets.write`

**Base URL:** `https://mex-pet-app.vercel.app/api/v1`

## Request

```bash
POST /pets
Authorization: Bearer mxp_live_your_token_here
Content-Type: application/json
```

## Body fields

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `contact_id` | string | ✅ | ID of the owning contact. Must exist in your business. |
| `name` | string | ✅ | Pet's name |
| `type` | string | — | Animal type: `dog`, `cat`, `bird`, etc. |
| `breed` | string | — | Breed |
| `age` | string | — | Age (free-form: `"3"`, `"18 months"`) |
| `weight` | string | — | Weight (free-form: `"65 lbs"`) |
| `gender` | string | — | `male` or `female` |
| `allergies` | string | — | Known allergies |
| `health_notes` | string | — | Free-text health information |
| `is_vaccinated` | boolean | — | Whether vaccinations are current |
| `status` | string | — | `active` (default) or `inactive` |
| `custom_fields` | object | — | Business-defined custom values. Send as a JSON object where **each key is a `field_key`** from your pet custom-field registry and **the value is the value for that field** (see shape below). Unknown keys are ignored; `checkbox` values are coerced to booleans. Discover your available keys with [`GET /custom-fields?entity_type=pet`](/api-reference/custom-fields/list-custom-fields). |

<Warning>
  `contact_id` must be an existing contact in your business. An unknown ID returns `400`: _"contact\_id does not match a contact in your business."_
</Warning>

### `custom_fields` shape

`custom_fields` is a plain JSON object of **key → value** pairs. The key is the `field_key` of a pet custom-field definition; the value is what you want to store.

```json
"custom_fields": {
  "microchip_number": "985112000123456", // text
  "is_service_animal": true,             // checkbox → boolean
  "food_brand": "Blue Buffalo",          // dropdown
  "daily_meals": 2,                      // number
  "adopted_on": "2023-04-01"             // date (YYYY-MM-DD)
}
```

## Example

```bash
curl -X POST https://mex-pet-app.vercel.app/api/v1/pets \
  -H "Authorization: Bearer mxp_live_your_token_here" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: c3d4e5f6-a7b8-9012-cdef-345678901234" \
  -d '{
    "contact_id": "usr_6f1c3d",
    "name": "Max",
    "type": "dog",
    "breed": "Golden Retriever",
    "age": "2",
    "weight": "55 lbs",
    "gender": "male",
    "is_vaccinated": true,
    "custom_fields": {
      "microchip_number": "985112000123456",
      "is_service_animal": false,
      "food_brand": "Blue Buffalo"
    }
  }'
```

## Response `201`

```json
{
  "id": "pet_bb2c3d",
  "object": "pet",
  "contact_id": "usr_6f1c3d",
  "name": "Max",
  "type": "dog",
  "breed": "Golden Retriever",
  "age": "2",
  "weight": "55 lbs",
  "gender": "male",
  "allergies": null,
  "health_notes": null,
  "is_vaccinated": true,
  "status": "active",
  "image_url": null,
  "custom_fields": {
    "microchip_number": "985112000123456",
    "is_service_animal": false,
    "food_brand": "Blue Buffalo"
  },
  "created_at": "2026-07-09T15:00:00.000Z"
}
```